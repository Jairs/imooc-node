extends ../layout
block content
    .container
        form.form-horizontal(method="post",action="/admin/cagetory")
             .form-group
                label.col-sm-2.control-label(for="inputCagetory") 电影分类
                .col-sm-10
                    input#inputCagetory.form-control(type="text",name="category[name]",value=category.name)
            .form-group
                .col-sm-offset-2.col-sm-10
                    button.btn.btn-default(type="submit") 录入
